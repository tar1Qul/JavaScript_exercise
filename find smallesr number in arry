let arr = [5, 10, 25, 8, 2];
let smallest = arr[0];

for(let i = 1; i < arr.length; i++){
    if(arr[i] < smallest){
        smallest = arr[i];
    }
}

document.write("Smallest number = " + smallest);
