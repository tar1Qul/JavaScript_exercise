function square(){

            let num = parseInt(prompt("Enter a number"));
            let result = num * num;

            document.write("The square of " + num + " is: " + result + "</br>");
        };

        square();
        square();
        square(); 
        square();
