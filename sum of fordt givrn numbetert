let n = parseInt(prompt("Enter how many numbers you want to sum:"));

            let sum = 0;
            let i = 1;

            while (i <= n) {
                sum = sum + i;
                i++;
            }

            document.write("The sum of the first " + n + " numbers is: " + sum);
