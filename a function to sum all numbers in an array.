function arraySum(arr){

    let sum = 0;

    for(let i = 0; i < arr.length; i++){
        sum = sum + arr[i];
    }

    return sum;
}

let numbers = [5,10,15,20];

document.write("Total = " + arraySum(numbers));
